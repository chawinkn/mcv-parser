interface:
  display_name: "Mcv Parser"
  short_description: "Repo-specific patterns and workflows for mcv-parser"
  default_prompt: "Use the mcv-parser repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true